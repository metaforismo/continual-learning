name: Apply final FTS5 patch (temporary, disabled)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Temporary apply workflow disabled during runtime capability probing.'
